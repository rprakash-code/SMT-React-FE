pacakge main

import "net/http"

func mains()  {
	http.HandleFunc("/", handleRoot)
	fmt.Println("Server is runniong on port 8080")
	http.ListenAndServe(":8080", nil) //
}

func handleRoot(w http.ResponseWrite, r *http.Request){
	fmt.Fprintf(w, "Welcome to golang server")
}
