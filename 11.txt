1. Insert into productname_org_divisions
sql
Copy
Edit
INSERT INTO productname_org_divisions (DivisionName, Description, IsActive)
VALUES 
    ('Sales Division', 'Handles all sales-related activities', 1),
    ('Marketing Division', 'Responsible for marketing strategies', 1),
    ('Finance Division', 'Manages financial operations', 0); -- Inactive Division
🚀 2. Insert into productname_org_departments
sql
Copy
Edit
INSERT INTO productname_org_departments (DepartmentName, DivisionId, CreatedDate, IsActive)
VALUES 
    ('Regional Sales', 1, GETDATE(), 1),
    ('Digital Marketing', 2, GETDATE(), 1),
    ('Accounts Payable', 3, GETDATE(), 0); -- Inactive Department
Note: Make sure the DivisionId exists in the productname_org_divisions table.
🚀 3. Insert into productname_org_roles
sql
Copy
Edit
INSERT INTO productname_org_roles (RoleName, Description, IsActive)
VALUES 
    ('Admin', 'Full access to all system features', 1),
    ('Division Manager', 'Manages division-specific data', 1),
    ('Department User', 'Access limited to assigned department', 1),
    ('Auditor', 'Read-only access for audits', 0); -- Inactive Role
🚀 4. Insert into productname_org_users
sql
Copy
Edit
INSERT INTO productname_org_users (Email, FullName, PasswordHash, IsActive, CreatedDate)
VALUES 
    ('john.doe@example.com', 'John Doe', 'hashed_password_123', 1, GETDATE()),
    ('jane.smith@example.com', 'Jane Smith', 'hashed_password_456', 1, GETDATE()),
    ('mark.jones@example.com', 'Mark Jones', 'hashed_password_789', 0, GETDATE()); -- Inactive User
Replace 'hashed_password_123' with the actual hashed password.
⚡ 5. Insert Example with Conditional IsActive Logic
If you want to insert data where IsActive is always 1 by default (without specifying it), ensure the column has a default constraint:

sql
Copy
Edit
ALTER TABLE productname_org_divisions
ADD CONSTRAINT DF_IsActive DEFAULT 1 FOR IsActive;
Then you can insert without explicitly mentioning IsActive:

sql
Copy
Edit
INSERT INTO productname_org_divisions (DivisionName, Description)
VALUES ('IT Division', 'Handles all IT-related operations');
🔍 6. Verify Inserted Data
To check the active records:

sql
Copy
Edit
SELECT * FROM productname_org_divisions WHERE IsActive = 1;
To view inactive records:

sql
Copy
Edit
SELECT * FROM productname_org_divisions WHERE IsActive = 0;
